<%= form_for(stock_type) do |f| %>
  <% if stock_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stock_type.errors.count, "error") %> prohibited this stock_type from being saved:</h2>

      <ul>
      <% stock_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :about %>
    <%= f.text_area :about %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
