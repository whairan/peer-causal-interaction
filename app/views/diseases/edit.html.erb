
<%= render partial: "partials/devise_navbar" %>

<div class="container content page-wrapper pagepad">
  <%= render "partials/flash" %>
  <p id="notice"><%= notice %></p>
  <div class="panel panel-primary">
      <div class="panel-heading">
        <h4 class="panel-title"><i class="fa fa-user-md" aria-hidden="true"></i> Editing Disease 
        </h4>
      </div>
      <div class="panel-body">
      	<%= render 'form', disease: @disease %>
      </div>
    </div>
  <br>
  <%= link_to '<i class="fa fa-arrow-left" aria-hidden="true"></i> Cancel'.html_safe, diseases_path, class:"btn btn-primary" %>

  <td><%= link_to '<i class="fa fa-eye" aria-hidden="true"></i> Show Disease'.html_safe, @disease, class:"btn btn-info" %></td>
</div>
